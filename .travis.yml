language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jhershauer/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jhershauer/docker-react npm run test
